version: "2"         # required to adjust maintainability checks
checks:
  argument-count:
    config:
      threshold: 10
  complex-logic:
    config:
      threshold: 10
  file-lines:
    enabled: flase
  method-complexity:
    config:
      threshold: 20
  method-count:
    config:
      threshold: 20
  method-lines:
    config:
      threshold: 50
  nested-control-flow:
    config:
      threshold: 20
  return-statements:
    config:
      threshold: 20
  similar-code:
    enabled: flase
  identical-code:
    enabled: flase
plugins:
  pep8:
    enabled: true
  pylint:
    enabled: true
  sonar-python:
    enabled: true
  radon:
    enabled: true
